# Agent 1 Instruction Profile: Layer 1 TradingStage
* **Focus Area:** Implement SRD 1 (`TradingStage`).
* **Isolation Boundary:** You are restricted to modifying code elements inside `core/src/trading_stage/`. Do not import layout, UI, or rendering crates.
* **Key Tasks:** Build the path-addressable primitive maps, structural multi-tier data stacking layer pipelines (`Base`, `Signals`, `Overrides`), dirty flag collection lists, and strict Serde serialization routines.
