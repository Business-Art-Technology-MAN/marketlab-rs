# Agent 2 Instruction Profile: Layer 2 ExecutionEngine
* **Focus Area:** Implement SRD 2 (`ExecutionEngine`).
* **Isolation Boundary:** You are restricted to modifying code elements inside `core/src/execution_engine/`. You interface with Layer 1 via public abstraction traits.
* **Key Tasks:** Build the mixed-frequency temporal stride arrays, forward-filling algorithms with zero future look-ahead leaks, topological graph compilation sorters, and simulation transaction account matrices tracking cash asset pools.
